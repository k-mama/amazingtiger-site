/**
 * Two-tier logotype: a bold serif name over a small, wide-tracked
 * subtitle — replaces the earlier single-line hollow gradient treatment,
 * which read as stretched too wide across the header.
 */
export default function WordmarkLogo() {
  return (
    <span className="wordmark__lines">
      <span className="wordmark__line1">Amazing Tiger</span>
      <span className="wordmark__line2">Publishing</span>
    </span>
  );
}
